# README

Simple cxx template generator with logging facilities included.


```bash
git clone --depth=1 https://github.com/xexaaron/CxxTemplate.git
cd CxxTemplate
# running without flags will start the interactive mode. (use -h to get flags)
# if you messup during the setup script you will have to reclone the repo and start again. 
./setup.sh   
```